{
  "name": "cloud-based-ci-cd-pipeline",
  "version": "1.0.0",
  "description": "Node.js application for CI/CD pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
